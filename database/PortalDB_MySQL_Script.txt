CREATE TABLE DOWNLOADS (DOWNLOAD_NUM INT unsigned not null auto_increment, SUBJECT Varchar(255) not null, LAST_MODIFIED DATE not null, DESCRIPTION MEDIUMTEXT, DOWNLOAD_URL Varchar(2048), DOWNLOAD_IMG MEDIUMBLOB, PRIMARY KEY (DOWNLOAD_NUM));
CREATE TABLE CONFIG_DATA (CONFIG_KEY Varchar(20), CONFIG_VALUE Varchar(255), PRIMARY KEY (CONFIG_KEY));
INSERT INTO CONFIG_DATA (CONFIG_KEY, CONFIG_VALUE) VALUES ('HIT_COUNTER', '0');
grant all on portaldb.* to lucasdb@localhost identified by '123456';